name: PR Checks

on:
  pull_request:
    branches:
      - main
      - develop
    paths:
      - 'reactapp/**'
      - '.github/workflows/pr-checks.yml'

jobs:
  lint-and-type-check:
    name: Lint and Type Check
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: reactapp/package-lock.json
          
      - name: Install dependencies
        working-directory: ./reactapp
        run: npm ci
        
      - name: Run ESLint
        working-directory: ./reactapp
        run: npm run lint
        
      - name: TypeScript type check
        working-directory: ./reactapp
        run: npx tsc --noEmit
  
  # test:
  #   name: Run Tests
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v4
  #       
  #     - name: Setup Node.js
  #       uses: actions/setup-node@v4
  #       with:
  #         node-version: '20'
  #         cache: 'npm'
  #         cache-dependency-path: reactapp/package-lock.json
  #         
  #     - name: Install dependencies
  #       working-directory: ./reactapp
  #       run: npm ci
  #       
  #     - name: Run tests
  #       working-directory: ./reactapp
  #       run: npm run test:run
  #       
  #     - name: Generate coverage report
  #       working-directory: ./reactapp
  #       run: npm run test:coverage
  #       
  #     - name: Upload coverage to Codecov
  #       uses: codecov/codecov-action@v4
  #       with:
  #         files: ./reactapp/coverage/coverage-final.json
  #         flags: reactapp
  #         name: reactapp-coverage
  
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [lint-and-type-check] # Add 'test' when tests are enabled
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: reactapp/package-lock.json
          
      - name: Install dependencies
        working-directory: ./reactapp
        run: npm ci
        
      - name: Build application
        working-directory: ./reactapp
        run: npm run build
        
      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: react-app-build
          path: reactapp/dist
          retention-days: 7
          
      - name: Check build size
        working-directory: ./reactapp
        run: |
          echo "## Build Size Report" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          du -sh dist/* >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
